# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。每个版本节点在 git 中打有
annotated tag，并在本文件记录变更。

**版本号单一来源**：`js/version.js`。改版本号时须同时在本文件顶部追加条目，
`tests/version-check.js` 会断言两者一致。页脚显示的版本号同样取自该文件。

对应站点：<https://wisdomhuang2020.github.io/optical-calculator/>

## [v3.6.0] - 2026-09-19

### 新增（第 5 页「棱镜原理」、第 6 页「材料与参考」）

把一份独立的《一维与二维棱镜板参数化设计与导出》单页文档的**知识部分**
合并进本站，成为第 5、6 个 tab：

| 新页面 | 内容 | 来源 |
|---|---|---|
| **第 5 页 · 棱镜原理** (`#know`) | 概览 + 光学原理 | 原文档「概览」「光学原理」两页合并 |
| **第 6 页 · 材料与参考** (`#mats`) | 设计校核 + 材料与规格 + 制造工艺 / 验证方法 + 参考资料 | 原文档「设计要点与材料」「参考资料」两页合并 |

第 5 页含三个 2D 光线追迹演示（准直原理 · 实时光线追迹、单棱镜光线追迹、
光线扇形图），均带滑块实时联动与读数；第 6 页含随棱镜工具参数实时更新的
设计校核清单、两张材料/规格表（保留逐条来源核查标注）与 9 条参考资料。

### 合并时做的四件事（每一件都对应一类失效）

1. **不逐字转录**。函数体是用脚本按花括号配对从原文档抽出来的，
   手抄 600 行必然出错。颜色与接线另做定点替换。
2. **零外部依赖**。原文档从 `cdnjs` 引 three.js —— 本站的硬约束是
   零外部依赖（被墙/离线时不能白屏）。三个演示都是 2D canvas，
   本就不需要 WebGL，该依赖直接不引入。
3. **颜色全部走 CSS 变量**。原文档自带 6 个基色 + 十余种透明度变体
   （`rgba(70,200,232,.45)` 这类共 38 处），已全部改写为
   `rgb(C.x)` / `R('x',α)`，基色定义收敛到新增的 `--c-ray-*` 系列。
   `index.html` 里 6 处内联硬编码色（图例与扇形图注）一并改为变量。
4. **样式锁在 `.kn` 作用域内**。该文档与本站有 **12 个重名类名**
   （`.note .hero .stats .readout .num .formula .legend .dot .k .v .u .wrap`），
   不锁作用域就会污染其它页面。做法是先把它的一套令牌名
   （`--cyan / --panel / --line / --ink …`）映射到本站令牌 —— 这样文档内部
   大量既有的 `var(--cyan)` 写法原样生效，无需逐处改写。

### 变更（棱镜板设计页吸收原文档的三项做法）

按用户确认，从原文档的「设计建模」部分抽出三项并入第 4 页。
**相机 / OrbitControls / 取景 / 材质一律未动。**

#### 1. 参数域校验 + 静默钳位上报

- **顶角 / 倾角的定义域此前根本没查**。一维顶角须在 (0°,180°) 开区间
  （0° 时齿退化成零宽，180° 时 `tan(α/2)→∞` 让半底宽发散），
  二维倾角须在 (0°,90°)；原先只查了 pitch / height / base / L / N。
- **计数类输入改为显式钳位并上报**。原写法 `Math.max(1, Math.round(x))`
  在超范围时静默改值：输入框里仍显示用户填的数，几何却按另一个数算。
  现记录进 `inputNotes`，由提示区回报「超出 1–500，已按 500 计算
  （输入框仍显示 600）」。

#### 2. 轮廓去重（真实隐患）

`2b = pitch` 是**物理上合法**的最密排（齿底恰好相接、没有平谷）。
此时相邻两齿的齿根点落在同一位置，轮廓出现重合的相邻点 ——
实测 `pitch=1 / h=0.25 / α=126.87°` 时有 **21 个**（索引 2,5,8,…）。

后果不只是"多几个点"：零长边围出的面面积为 0，面法线经 `nrm()` 的
`|| 1` 兜底变成**零向量**，于是 STEP 里写出
`DIRECTION('',(0.000000,0.000000,0.000000))` —— 而 DIRECTION 按定义必须是
单位向量。**实测该参数下 108 个侧面里有 63 个轴系非法**（正常参数为 0）。
现加 `dedupePts()` 在剖分源头去重，三个角度实测 `zeroDir` 全部归零。

#### 3. 导出前置检查三件套

- **STEP 体积闸门**：先按截面点数 / 三角形数**估算**（一维 900 B/点、
  二维 800 B/三角面），超过 12 MB 先确认再生成 —— 若先生成后确认，
  用户要多等十几秒才看到对话框，白等。
- **剪贴板降级**：`navigator.clipboard` 只在安全上下文（https / localhost）
  可用，纯静态站完全可能被以 `file://` 打开，此时原实现既无 else 也无
  `catch`，点「复制」**静默失败**（按钮文字都不变）。现补
  `execCommand` 降级，并把失败明确显示出来。
- **STL 按 `valid` 门控**：原先只判 `mesh` 是否存在，参数变非法后仍会导出
  **上一版旧网格**，与 STEP 按钮的行为不一致。

### 测试

- 新增 `tests/knowledge-check.js` 独立套件（48 项）：静态部分守
  零外部依赖、样式作用域（12 个重名类名必须带 `.kn` 前缀）、颜色走变量、
  脚本顺序与 hash 白名单；运行时部分用无头 Chrome **量画布像素**
  （初始化守卫写错 id 会让三个演示全空而页面与控制台都毫无反应）、
  验滑块联动与读数、校核清单与参考资料是否真的渲染出来。
- `tests/prism-check.js` 新增第 9 组「参数单位与物理定义标注」（见 v3.5.1）。
- `tests/run-all.js` 现为 **10 个套件**；`pages.yml` 的发布文件校验清单
  加入 `js/prism-knowledge.js`（该工作流的既有惯例：新增文件必须显式校验，
  否则漏了会静默失效）。
- 全部 10 个套件通过。

## [v3.5.1] - 2026-09-19

### 修复（参数面板：单位、物理定义、以及「实际生效值」）

用户看着 `radius 圆角 = 1.57` 问「这个用的是弧度角吗」。面板原先只在标题写了
「阵列参数 (MM)」，单个字段既没有单位也没有定义 —— 一个**长度量**
（过渡圆弧半径 R）很容易被读成角度。而真正的缺陷在后面：**这个数根本没生效**。

#### 1. 每个参数标明单位与物理定义

- 全部数值字段补上单位：`mm` / `°` / `个` / `mm · 拉伸向`。
- 每个字段下方加一行定义说明（`.param-hint`），写清这个量是什么：
  - **radius 圆角**：齿顶与齿根过渡圆弧的**半径 R**，是**长度**（mm），
    **不是角度、不是弧度**；并说明它受齿边长度限制。
  - **apex angle 顶角**：齿顶处两条齿面之间的夹角 α，是**全角**
    （不是半角 α/2，也不是斜面与底面的夹角），齿半底宽 `b = h·tan(α/2)`。
    本项目在光束角上吃过半角/全角的亏（见 v3.0.0 的 BREAKING CHANGE），
    这里提前把口径写死。
  - **二维的 apex angle 是「斜面倾角」**，与一维的顶角**不是同一个量**，
    说明里点明了这一点（`b = h/tan(α)` vs 一维的 `h·tan(α/2)`）。

`.field` 是两列网格（标签 | 输入框），说明文字是第三个子元素，必须显式
`grid-column: 1 / -1` 才会跨列 —— 否则会被塞进下一行第一列变成窄条。

#### 2. 圆角被钳位却毫无提示（本版实质缺陷）

`filletRadii()` 按两条规则钳位：`rr = min(R, 0.45·l1, 0.45·l2)`，以及
**同一条边上两端切线长之和 `T_u + T_v ≤ 边长`**。于是 R 超过某个阈值后
**完全不再生效**，且各角点饱和值不同（齿顶与齿根内角不同，`T = rr/tan(θ/2)`
随之内不同）。实测 `pitch=1 / h=0.25 / 顶角 60°`（齿边仅 0.289 mm）：

| 输入 R | 齿顶实际生效 | 比例 |
|---|---|---|
| 0.05 | 0.050 mm | 1.00× |
| 0.1 | 0.090 mm | 0.90× |
| 0.2 | 0.090 mm | 0.45× |
| **1.57** | **0.090 mm** | **0.057×** |
| 3.1416 | 0.090 mm | 0.029× |

填 1.57 与填 0.2 得到的是**同一个形状**，而页面原先对此一字未提 ——
用户只会觉得「改了没反应」，这正是本版本系列一直在修的那一类
「输入 ≠ 实际建模，且静默」。

现改为：

- **几何量新增一行「圆角 R（实际生效）」**，给出实际值；各角点不一致时
  给区间（默认参数下为 `0.090 ~ 0.125 mm`），不藏差异。
- **钳位时给出可见告警**（琥珀色告警框，与「齿底重叠」告警同一处）：
  ```
  圆角半径 R = 1.57 mm 超出该齿形能实现的尺寸（齿边长度仅 0.289 mm），
  实际生效 0.090 ~ 0.125 mm —— 再增大 R 也不会改变形状。
  要得到更大的圆角请增大 pitch / height，或减小顶角。
  ```
- 告警改为**多条并存**（`.warn-box` 加 `white-space: pre-line`，
  否则 `\n` 会被折叠成一行）。原先只有一个 `msg` 变量、后写的覆盖先写的。
- `geo` 增加 `rMin` / `rMax` / `rClamped`，使「实际生效值」可被断言而非目测。
- **二维隐藏该行**（金字塔阵列没有圆角、面板也没有该输入）——
  留一个「—」在彼处反而会被读成「二维圆角是 0」。

### 测试

- 新增 `tests/prism-check.js` 第 9 组「参数单位与物理定义标注」12 项静态门禁
  （把「标注本身」作为门禁：单位是否齐全、定义是否写明「不是角度/不是弧度」、
  `.param-hint` 是否跨列等）。
- 新增 `tests/smoke-render.js` 第 12 组「radius 圆角」7 项运行时断言 ——
  真的把 `p_radius` 改成 1.57 再读回：几何 `rMax` 是否被钳、页面读数是否
  与几何一致（而非照抄输入）、告警是否真的出现、复原后是否不残留。
- 冒烟测试 116 项全绿，9 个套件全绿。

## [v3.5.0] - 2026-09-19

### 修复（棱镜板：参数与几何/渲染对不上）

用户报「一维设计参数与渲染图展示对不上」。逐项核查后确认这不是显示问题，
而是**参数根本没参与建模** —— 一维的顶角、二维的倾角都被几何忽略，
页面照常显示、按钮照常响应，只是画出来的东西和填进去的数无关。
另修掉取景的两处缺陷，它们让棱镜板被渲染成「一堵竖墙」。

#### 1. 一维：顶角完全不参与建模（最严重）

`rawProfile()` 里算出了 `half = h/tan(α/2)` 却**从未使用**，
齿根直接落在 `i·pitch` 上，即齿底宽恒等于一个整齿距。

- **实测**：把顶角从 20° 扫到 150°，生成的 43 个截面点**逐点完全相同**；
  实际齿顶角恒为 `2·atan(pitch/2h)` = **126.87°**（默认参数），
  与输入的 60° 毫无关系。
- **修复**：齿底宽改为 `2b = 2h·tan(α/2)`，齿居于所属齿距中央，
  齿间保留 `pitch − 2b` 的平台（相邻齿不接触）。这正是棱镜膜的实物形态。
- **实测**：修正后齿实际顶角 = 输入顶角，逐位吻合；截面点数 43 → 64（= 3N+4）。

顶角一变，几何立刻跟着变：

| 顶角 α | 半底宽 b = h·tan(α/2) | 填充率 η = 2b/p | 体积（L=50） |
|---|---|---|---|
| 30° | 0.0670 mm | 0.134 | 215.9 mm³ |
| 60°（默认） | 0.1443 mm | 0.289 | 235.8 mm³ |
| 90° | 0.2500 mm | 0.500 | 262.4 mm³ |

#### 2. 一维：半底宽读数是正确值的**倒数**

`half` 写的是 `h / tan(α/2)`。顶角 60°、齿高 0.25 时它给出 **0.433 mm**，
而按顶角的定义应为 `h·tan(α/2)` = **0.144 mm**；几何真实的半底宽
（旧几何下）又是 `pitch/2` = 0.5 mm —— 三个数互不相等，且没有任何一个
和渲染出来的东西有关系。

现 `halfBase()` 是半底宽的**唯一定义点**，`rawProfile`（几何）与
`buildGeometry`（读数）共用它，从结构上消除漂移。

#### 3. 一维：「面/边数」那一格显示的是底角 β

`index.html` 的标签写「面/边数」，代码却往里写 `(180−α)/2`，
页面显示 `60.00 °`（那是棱镜斜面相对底面的夹角）。真正的顶点数在下一格。
另外 `LABELS_1D` 声明了却从未被调用，是死代码。

现改为 6 格各自单点定义：**板宽 W = N·p / 总高 H = t+h /
半底宽 b = h·tan(α/2) / 填充率 η = 2b/p / 网格点 / 体积**，
两个模式的标签都随模式切换刷新。

#### 4. 二维：倾角同样不参与建模，且体积是真实值的 4 倍

网格点固定在 `(i·pitch, j·pitch)`、金字塔顶点固定在格心，
底面恒为 `pitch×pitch` 满铺、四周没有平台；体积按 `pitch²` 算。

- **实测**：默认倾角 45° 时 b = 0.25，金字塔底面应为 0.5×0.5，
  体积 8.333 mm³；而按整齿距算得 33.33 mm³ —— **4 倍**。
  整板体积由 113.3 → **88.3 mm³**（基底 80 + 金字塔 8.33）。
- **修复**：底面改为 `2b×2b`（`b = h/tan(倾角)`，倾角即斜面与水平面的夹角），
  四周留 `pitch − 2b` 平台。网格与 STEP 两侧同步改造，
  每格 12 个三角形（金字塔侧面 4 + 平台环 8），与读数同源（`triCount2D()`）。

注意一维与二维的半底宽公式**本来就该不同**：一维的 α 是齿顶夹角
（`b = h·tan(α/2)`），二维的 α 是斜面倾角（`b = h/tan(α)`）。
原文案把两者写成同一个 `h/tan(α)`，掩盖了这个区别。

#### 5. 圆角半径超过齿尺寸时会自交（本次收窄齿底宽后暴露）

切线长 `T = rr / tan(θ/2)`，尖角处 `tan(θ/2)` 很小 —— 齿顶 60° 时
`T = 1.73·rr`。于是同一条边上相邻两个圆角的切点会互相越过
（默认 `radius = 0.2` 时 `1.73 × 0.2 = 0.35 >` 单边斜长 0.29），
采样弧随之翻向，轮廓自交、面积与体积一起失真。

原钳位 `rr = min(r, 0.45·l1, 0.45·l2)` 只约束半径、不约束切线长，挡不住。
现抽出 `filletRadii()` 作为半径与切线长的唯一定义点，并**按边施加**
`T_u + T_v ≤ 边长`，逐顶点取两条邻边约束的最小系数一次性施加。
450 组参数网格扫描下不再出现任何溢出边。

#### 6. 取景：板子被渲染成「一堵竖墙」

两处独立缺陷叠加：

- **相机 up 与世界 Z 不一致**。`frameObject()` 的取景数学本来就是
  Z-up 约定（`right = fwd × Z`、`up = right × fwd`），但相机从未设过
  `up`，默认是世界 Y。后果：板子的长边（世界 Y）被**永远投影成竖向**，
  20×50 的板子渲染成竖墙；取景算出的距离也与真实视锥不符 ——
  实测模型投影仅 124×381 px、只占画面宽 **13.6%**。
  现显式 `camera.up.set(0, 0, 1)`，且必须在构造 OrbitControls **之前**
  （它内部按 `object.up` 一次性算出轨道坐标系四元数）。
- **取景迭代只能推远、不能拉近**。`frameObject` 里 `var need = 0`
  与 `Math.max` 配合，把"装得下时 need 为负"这一半语义截掉了；
  而初值 `3×包围盒对角线` 本来就偏远，于是第一轮 `need = 0` 直接 break，
  相机被钉死在 1.8 倍于所需的距离上。现改为 `var need = -Infinity`。
- 另补**投影包围盒居中**：透视下「盒子中心的投影」不等于「投影的中心」，
  实测垂直偏移 41 px 恰好超出居中判据。现按 NDC 实测偏移迭代平移相机
  与注视点（必须扣掉累计平移量，否则第二轮会重复修正一次、正好过头一倍）。

**取景实测**（默认参数，912×520 舞台）：投影 124×381 → **558×310**，
横向占比 13.6% → **61.2%**，居中偏移 (5, 41) → **(0, 0)**，
四边余量 L129 R131 T85 B85。

#### 7. 三维预览材质：高光玻璃感把棱面结构糊掉了

按反馈「二维渲染不要这么反光，表面暗一些更容易显示结构细节」调整。

原材质是 `color 0x9ecbff / roughness 0.18 / clearcoat 1.0 /
clearcoatRoughness 0.08 / opacity 0.82`（半透明 + 清漆层）——
对一维的连续肋面尚可，但**二维金字塔阵列是大量朝向各异的小平面**：
高光与清漆会在每个面上各打一块高亮，相邻面一起过曝成白，棱面之间的
明暗差异被抹平；半透明又透出背面，进一步糊掉层次。

现改为哑光不透明，并给二维开平面着色：

| 项 | 旧 | 新 | 作用 |
|---|---|---|---|
| roughness | 0.18 | **0.9** | 压掉镜面高光，亮度改由面朝向决定 |
| clearcoat | 1.0 | **去除** | 去掉第二层镜面反射 |
| opacity | 0.82（半透明） | **1.0（不透明）** | 不透出背面，保住明暗层次 |
| flatShading | false | 二维 **true** / 一维 false | 每个三角形用自己的面法线 |

`flatShading` 对二维是关键：不开启时共享顶点的法线会被平均，塔尖呈圆滑
过渡、棱边消失。一维**刻意不开** —— 它的圆角是 10 段折线拟合的，
平面着色会把圆角显成 10 个折面，反而更假。

灯光同步重配：环境光 0.35 → **0.22**、主光 1.1 → **1.25**、
轮廓光 0.5 → 0.42、补光 0.3 → 0.22。环境光过高会把所有朝向的面一起抬亮，
棱面之间就没有明暗差；补光保留少量，否则背光面会黑成一片看不出轮廓。
这套强度原先是为玻璃感调的（那时亮度主要来自高光，环境光高低不敏感）。

颜色仍走「语义色单一来源」：新增 `--c-prism`（表面基色）与
`--c-prism-key` / `--c-prism-rim` / `--c-prism-fill`（三盏灯的颜色），
原先硬编码在 prism.js 里的 `0x9ecbff` 与灯光色一并收进 styles.css ——
**3D 观感的调参现在集中在一处**。透视切换同步修正：`transparent` 与
`opacity` 必须一起改并置 `needsUpdate`（常态是不透明，`transparent=false`
时 opacity 会被忽略），原实现只改 opacity，故「半透明」按钮在改成不透明
材质后会失效。

### 兼容性说明

- 输出的几何与体积**变了**：一维默认参数 325.0 → **235.8 mm³**，
  二维 113.3 → **88.3 mm³**。前者是"齿底白占一个齿距"的虚高，
  后者是金字塔底面按整齿距算的虚高。已在模板库里用过旧 STEP 的需重新导出。
- 一维顶角现在**真的会改变齿形**。此前改动它除了读数什么都不影响 ——
  若历史上有脚本依赖「改顶角几何不变」，那一行为已不成立。
- 导出的 build123d / CADQuery 脚本同步修正（新增 `HALF_B` 参数，
  截面点按 `cx ± HALF_B` 生成），与页面几何一致。

### 测试

- 新增 `tests/smoke-render.js` 第 10 组「顶角/倾角必须真正参与建模」7 项：
  同时查**读数**与**真实几何**（`Prism.geo.half`、截面点签名、体积），
  只查读数挡不住「读数对了、几何没动」这一类。另加取景 2 项
  （取景宽高比 = 真实视口宽高比、横向占比 ≥ 45%）。
  截面点签名取轮廓**中段** —— 前几个点是板角，与顶角无关，
  只看它们会得出「改顶角没变化」的假结论。
- 新增 `tests/prism-check.js` 第 7 组「参数↔几何一致性」13 项、
  第 8 组「三维预览材质」11 项静态门禁（含「断言须排除注释」——
  旧值的十六进制写在说明性注释里，全文搜会误判成"仍然硬编码"）。
- 冒烟测试新增第 11 组「材质：哑光不透明」7 项运行时断言
  （`Prism.probe()` 现额外暴露材质参数，使 roughness / 清漆 / 透明度 /
  平面着色都可断言，不再只能靠截图目测）。
- STEP 结构断言随新几何更新：圆弧 86 → **128**（64 个圆角 × 2 端环）、
  圆柱面 43 → **64**、`EDGE_LOOP` 131 → **194**、`VERTEX_POINT` 258 → **384**。
- 冒烟测试 110 项全绿，9 个套件全绿。

## [v3.4.1] - 2026-09-18

### 修复（CI 上"看不出为什么失败"）

v3.4.0 的部署卡在 CI 的「全部测试套件」一步：本地用同一套命令、
同样的环境变量（`REQUIRE_OCP=1 REQUIRE_CHROME=1`）跑 8 个套件全绿，
CI 却失败，而**拿不到日志**（Actions 日志接口返回 403、注解为空），
check-run 里也只有"步骤失败"四个字。排查因此长时间停留在猜测。

本版不去猜病因，转而**把"无法诊断"本身当缺陷修掉**：
让测试体系在失败时自己说清原因。

#### 1. 浏览器定位**静默回退**（本轮实测抓到的真缺陷）

`findChrome()` 原来是「按候选顺序试，第一个存在的就用」。实测：

```
CHROME_BIN=/nonexistent/chrome node tests/...   → 全绿
```

非法路径被静默丢弃，代码继续往下找到系统 Chrome 并使用 ——
**测试通过，但用的是另一个浏览器**。在 CI 上这正是"失败、又复现不了"的温床：
`CHROME_BIN: ${{ steps.chrome.outputs.chrome-path }}` 若为空或指错，
测试不会报"Chrome 找不到"。

现改为：**显式指定的路径必须可用，否则直接判负，绝不回退**；
未显式指定时才按候选表探测。工作流侧同时加一步校验 `chrome-path` 非空且可执行。

#### 2. 汇总必须带**断言总数**

原来打印「通过 N 项，失败 M 项」。`0 通过 + 0 失败` 也满足"全绿"——
若某轮所有断言都被跳过，汇总照样一片祥和。现统一为
`通过 N 项，失败 M 项（断言总数 T）`，且 `T = 0` 直接判负；
被跳过的项单列计数并提示「这些性质未被验证」。

#### 3. 失败原因被自己淹没

- 某条失败的细节里带了 Chrome 完整 stderr（实测几千字多行），
  把汇总里其它失败项全挤出屏幕。现统一压成单行、截断 160 字。
- `smoke-render.js` / `step-export.js` 调 Chrome 时 `stdio` 的 stderr 是
  `'ignore'`，`catch` 里只打 `e.message`（"Command failed: …"）——
  真正的病因（沙箱、共享库、虚拟时间预算不足被截断）全被吞掉。现改为捕获并打印
  前 6 行，同时打印完整命令与探针路径。DOM 长度异常短时立即判负并单独说明，
  避免几十条 `got=null` 把"DOM 是空的"这个真因埋掉。
- 探针 HTML 固定文件名 → 改为带进程号，避免残留/并发互相覆盖。

#### 4. 新增环境体检，CI 失败先分清"环境"还是"代码"

新增 `tests/env-probe.js`，独立成 CI 的一步，只报告事实：
Node/Python 版本、`CHROME_BIN` 等环境变量实际取值、13 个关键文件是否齐全、
Chrome 与 OCP 是否真的可用（并**真做一次最小无头渲染**验证）。
输出一律以 `ENV ` 前缀，便于在日志里 grep。
它**刻意不跑 `chrome --version`**：Windows 上 Chrome 是 GUI 程序，
该参数会拉起完整浏览器、把 USB/GCM 报错灌满输出且迟迟不返回（实测数十秒），
既拿不到版本又污染日志 —— 直接测"能否无头渲染"才是我们唯一关心的能力。

#### 5. `run-all.js` 失败时回放输出

套件输出改为落盘保留（原来 `stdio:'inherit'` 一失败就丢失），
失败时各回放尾部 40 行；并区分"断言失败"与"进程异常退出/被信号中断/超时"
（单套件加 15 分钟上限，防无头 Chrome 挂死时整轮无限等）。
CI 日志拿不到时，终端里这份回放就是唯一证据。

#### 6. 新增 `tests/harness-check.js`（30 项）

守护上述**诊断设施自身**，防止下次被改回去：汇总须含断言总数、
显式指定工具路径不得静默回退、三个套件必须共用 `lib/find-tool`、
`run-all` 须落盘并回放、Chrome 调用须捕获 stderr。
其中 4 项是**运行时行为验证** —— 真的用非法 `CHROME_BIN` 跑一次 `env-probe`
和 `run-all`，断言退出码非 0 且报出明确原因。静态断言挡不住
"代码写着不回退、实际还是会回退"这类问题，必须真跑。

### 说明（未改动任何产品代码）

本版**只改测试体系与 CI 工作流，`js/` 下产品代码一行未动**。
棱镜板页的几何与 STEP 导出逻辑与 v3.4.0 完全一致，
`tests/step-export.js` 的 16 项读数（`TransferRoots=1`、`IsValid=True`、
圆柱面半径 0.02、体积 324.985648 mm³ / 113.333366 mm³）逐项未变。

即：v3.4.0 对审核报告的修复结论**全部维持**；本版解决的是
"这些结论在 CI 上无法被可信地验证与诊断"。

## [v3.4.0] - 2026-09-18

### 修复（棱镜板 STEP 导出：圆角是假的 + 内核读不进）

针对一份针对「棱镜板设计」页面的审核报告逐条核实，确认并修复三处缺陷，
其中两处在报告之外自行发现（见后两节）。

#### 1. 圆角在 STEP 里根本不存在（报告属实）

- **现象**：导出的 1D 文件里 `CIRCLE` 出现 0 次、`CYLINDRICAL_SURFACE` 0 次，
  只有 `PLANE` 432 + `LINE` 1290。页面上的 `radius` 圆角仅在 Three.js 预览里
  用 10 段直线拟合可见，**下载到的文件没有任何圆角**。
- **修复**：`filletDetailed()` 现同时返回每个圆角的圆心与半径，
  `buildSTEP()` 据此把采样点**压缩回拓扑**（43 个圆角 × 每个 9 段合并成 1 条圆弧边，
  129 条边 = 43 弧 + 86 直线），并写出**有理二次 B 样条**表示精确圆弧：

  ```
  #n = ( BOUNDED_CURVE() B_SPLINE_CURVE(2,(#P0,#P1,#P2),...)
         B_SPLINE_CURVE_WITH_KNOTS((3,3),(0.0,1.0),...)
         CURVE() GEOMETRIC_REPRESENTATION_ITEM()
         RATIONAL_B_SPLINE_CURVE((1.0,w1,1.0)) REPRESENTATION_ITEM('') );
  ```

  侧面同步写为 `CYLINDRICAL_SURFACE`（轴平行拉伸方向）。
- **实测**：用真实 OpenCascade 读回，`CYLINDRICAL_SURFACE = 43`，
  **全部半径精确等于 0.02 mm**（即输入的 `radius`）。

  为什么不用 `CIRCLE` + `TRIMMED_CURVE`：STEP 里 `CIRCLE` 语义是「整圆」，
  表达其中一段必须靠 `TRIMMED_CURVE` 钉参数区间，而 trim 点/参数、
  `sense_agreement`、轴系三者的组合约定极易踩错 —— 实测扫角翻成补弧
  （264°~306° 的优弧，材料被多切一大块），调整过程中还让 OCCT 在
  `TransferRoots` 直接崩。有理 B 样条把「这一小段弧」写进曲线自身定义，
  不含裁剪语义，各内核读法一致。

#### 2. 圆角圆心算错，凹角被当成凸角（报告未提，自行发现）

- **现象**：修复圆角表示后，体积反而比无圆角理论值**更离谱**。
  用单角独立圆角法做交叉验证发现：正确圆角只应把截面积从 6.5000 削到
  **6.49976 mm²**（−0.0037%），而原实现得到 **6.38418 mm²**（−1.8%）——
  少算了约 **26 倍**的材料。
- **根因**：圆心用「两切线单位向量之和」定方向，该式对凸角与凹角给出的是
  **同一侧**的角平分线，于是凹角（齿根）的圆心落到材料外，采样弧朝内凹，
  把本应**补上**的缺口反而又挖掉一块。
- **修复**：改由切点沿**入射边的内侧法线**偏移半径定圆心，
  法线方向按转向符号取（凸角取左法线、凹角取右法线）：

  ```
  nIn = turn > 0 ? [-d1y, d1x] : [d1y, -d1x]
  c   = a + rr · nIn
  ```

- **实测**：修正后凹角与凸角的圆心分居两侧（扫角分别为 −53.13° / +53.13°），
  密集采样的截面积收敛到 **6.499762210 mm²**，与解析式**逐位吻合**（9 位小数）。

#### 3. 显示值与导出件口径不一（报告属实）

- 页面显示的体积原取自**折线近似**的鞋带公式（6.397425 mm² → 319.87 mm³），
  而导出件走真圆弧 —— 「显示正确」与「文件正确」是两件事。
  现新增 `exactArea()`：无圆角多边形面积 + 逐角按真实圆弧的解析增减量，
  显示值与导出件同源。
- 顺带修正 `exactArea` 自身的两处公式错误：扇形圆心角应取**外角** `|turn|`
  而非内角 `θ`（对 126.87° 的齿角误差达 2.4 倍）；凹角不能整体取反号。

#### 4. 内核静默失败 → 已可正常读入（报告属实）

- **现象**：OCCT `ReadFile` 返回 `RetDone`，但 `TransferRoots() = 0`、
  `NbShapes() = 0` —— 不报错但从头到尾没读进任何几何，用户会以为导出成功。
- **根因**：三处参数元数（arity）错误：
  - `APPLICATION_CONTEXT('…',1)` —— 该实体只接受 **1** 个参数；
  - 裸 `GEOMETRIC_REPRESENTATION_CONTEXT(3)` —— 必须与
    `GLOBAL_UNIT_ASSIGNED_CONTEXT` 组成**复合实体**才合法；
  - `ADVANCED_BREP_SHAPE_REPRESENTATION` 只有 2 个参数
    —— 需要 3 个（name、items、context），原代码把上下文塞进了 items。
- **修复**：纠正三处元数，并**显式声明单位**（mm / rad / steradian）。
- **实测**：修复后 `TransferRoots = 1`、`NbShapes = 1`、`SOLID = 1`、
  `BRepCheck_Analyzer.IsValid() = True`，体积 **324.99 mm³**，
  与页面显示 325.0 mm³ 一致。

#### 5. `buildSTEP()` 不按模式分派（报告未提，自行发现）

- `window.Prism.buildSTEP()` 始终调用 1D 生成器，在二维模式下访问
  `geo.prof` 抛 `TypeError`。现按 `mode` 分派。

### 审核报告中**不成立**的两条

对报告逐条核实，以下两条经实测**否定**（保留在此以便追溯）：

- **「缺装配实体：`APPLICATION_PROTOCOL_DEFINITION`、`PRODUCT_CONTEXT`、
  `PRODUCT_DEFINITION_FORMATION`、`PRODUCT_DEFINITION_CONTEXT` 全无」**
  —— 后三者各存在 1 个（`PRODUCT_CONTEXT` / `PRODUCT_DEFINITION_FORMATION` /
  `PRODUCT_DEFINITION_CONTEXT` 原本就在文件里）。仅
  `APPLICATION_PROTOCOL_DEFINITION` 确实缺失，但它是**可选**实体，
  当前表示方式不需要它。
- **「端盖缺一条闭合边：`EDGE_LOOP` 列了 430 条边，而轮廓是 430 点开折线，
  首尾不重合、中间有 19.96 mm 跳变，需要 431 条边」**
  —— `rawProfile()` 实际是 **43 个点**（不是 430；430 是圆角**采样后**的点数）。
  圆角后轮廓经拓扑压缩为 129 条边，端盖 `EDGE_LOOP` 成员数 = 129，
  与侧边数一致；末点→首点距离恰为 **0.200000 mm**（即 `base` 厚度，
  是合法的左立壁），并非「19.96 mm 跳变」。
  校验器已断言 131 个环全部首尾相接。

### 工具与测试

- 新增 `tools/step_validate.py`：**零依赖** STEP 结构校验器 ——
  分节完整性、编号连续性、悬空引用、每个 `EDGE_LOOP` 的边链闭合、
  封闭壳的**边平衡**（每条 `EDGE_CURVE` 恰好被 2 个面引用 = 水密）、
  几何/曲面类型统计、产品结构普查。
  本版修正了它自身的两处缺陷：解析正则不认识**复合实例**
  （`#n = ( … )`，无单一类型名），导致误报编号空洞；以及无法在复合实例内
  数到 `GLOBAL_UNIT_ASSIGNED_CONTEXT`。
- 新增 `tools/step_occ_check.py`：用真实 OpenCascade 读回 STEP，
  报告 `ReadFile` 状态、`TransferRoots`、`NbShapes`、实体/面/边/顶点计数、
  体积、`BRepCheck_Analyzer.IsValid()`、曲面类型分布。
- `tests/smoke-render.js` 新增 **22 条 STEP 结构断言**，把上述缺陷逐项设卡，
  防止回归：`#undefined` 计数为 0；1D 圆弧数 = 86、圆柱面数 = 43、
  `CIRCLE` = 0；`EDGE_LOOP` = 131、`VERTEX_POINT` = 258；
  产品结构实体齐全；单位已声明；`APPLICATION_CONTEXT` 参数数 = 1；
  `ADVANCED_BREP_SHAPE_REPRESENTATION` 为 3 参数；2D 无圆弧且结构完整。
  原先只有一条 `stepLen > 1000` 的弱断言 —— 圆角全是假的时候它照样通过。
- 体积断言由 319.9 更正为 **325.0 mm³**（旧值对应折线近似，量级本身就错）。

### 验证矩阵（默认参数：pitch 1.0 / h 0.25 / 60° / t 0.20 / r 0.02 / N 20 / L 50）

| 量 | 页面显示 | OCC 读回实体 | 解析预期 |
|---|---|---|---|
| 截面积 | — | — | 6.499762210 mm² |
| 体积 | 325.0 mm³ | **324.985648 mm³** | 324.98811 mm³ |
| 圆弧曲线 | — | 86 条有理 B 样条 | 43 角 × 2 端环 |
| 圆柱面 | — | 43 个，半径 **0.02** | 输入 radius |
| 实体有效性 | — | `IsValid() = True` | — |

## [v3.3.0] - 2026-09-18

### 变更（棱镜板页的视角保持）
- **固定视角后改参数不再被复原**。上一版的取景判据是「包围盒尺寸签名变化即重新取景」，
  而改任何参数（pitch / 齿高 / 齿数 / 板长 / 圆角…）都必然改变包围盒尺寸 ——
  于是用户把视角调好之后一改参数，相机立刻被拉回标准 3/4 视角。
  现改为以**模式**为唯一的取景触发条件：同一模式内模型怎么变都不动相机，
  用户手动调整的视角因此天然被保留。
- 明确的取景规则（按实际使用反馈确定）：

  | 场景 | 行为 |
  |---|---|
  | 首次进入某个模式 | 自动取景到标准 3/4 视角 |
  | 手动转过视角后改参数 | **相机完全不动**，只重建模型 |
  | 切换一维 ↔ 二维 | 仍然自动取景（换的是完全不同的模型，不重置会跑到视野外） |
  | 点「重置视角」 | 回到标准视角，并恢复自动取景 |
  | 点「恢复默认」 | 参数与视角一并复位 |

- 重复点击当前模式按钮不重置视角（避免误触把调好的视角弄丢）。

### 新增（测试）
- 冒烟测试新增「视角保持与自动取景规则」4 项断言（69 → 73），判据直接读
  `camera.position` 三元组，覆盖两条**方向相反**的规则：同模式改参数相机必须不动、
  切模式必须重新取景。只断言其中一条都不够 —— 前者防"乱动"，后者防"不动"。
- 冒烟测试补了一条**探针自证**断言：模拟拖拽必须真的能改变相机位置。
  否则「改参数后相机没动」可能只是因为拖拽压根没生效，是个假阳性。
  踩到的坑：OrbitControls 的 `onPointerDown` 里 `switch(event.pointerType)`
  只认 `"mouse"`/`"pen"`，合成事件缺 `pointerType` 会被**静默忽略**。
- `js/prism.js` 的 `Prism.probe()` 增加 `camPos` / `camTarget` / `frameMode` 三个字段，
  使"相机有没有被动过"成为可直接断言的量；新增 `Prism.spin()` 用于在无头环境
  同步驱动 OrbitControls 的阻尼收敛（rAF 被节流时阻尼永远落不了地）。

## [v3.2.0] - 2026-09-18

### 新增
- **并入「棱镜板设计」页**（原 `MLA_Prism.html`）：一维棱镜肋 / 二维金字塔阵列的
  参数化设计，实时三维预览，导出 STEP（AP214）、build123d 脚本、CADQuery 脚本与
  预览用 STL。并入为本站第 4 个 tab，导航顺序：照度计算 / 立体角可视化 /
  公式与推导 / 棱镜板设计。
- **站点更名为「光学概念和光学设计」**，页头、页脚、`<title>` 三处同步。

### 变更（并入时的三项必要改造）
- **three.js 与 OrbitControls 本地化**。原文件从 `cdn.jsdelivr.net` 引入；本项目
  的硬约束是零外部依赖，CDN 不可达时会整页白屏。已落盘为
  `js/vendor-three.min.js`（603 KB）与 `js/vendor-orbitcontrols.js`（26 KB，
  均为 MIT），改为本地引入。
- **语义色单一来源**。原文件自带一套独立色板（`--bg:#0e1116` / `--accent:#4ea1ff`），
  与本站 `--bg:#0a0a0a` / `--primary:#14b8a6` 不同源。`js/prism.js` 内所有颜色
  改由 `cssVar('--c-*')` 读取，新增 `--bg-3d` 作为三处 3D 底色的唯一定义点。
- **画布尺寸自愈**。原文件在容器尚未布局时初始化 WebGLRenderer，
  实测**画布高度停在 20px**；`resize()` 原本量的是视图容器（高度由内容撑开），
  改为量固定高度的宿主 `.prism-stage`，并在切到本视图与窗口 resize 时重新调用。

### 修复（并入过程中实测抓到的缺陷）
- **控件全部静默失效**：`js/prism.js` 以 `.prism-view` 为作用域，
  而视图元素的 class 是 `view`（`id="view-prism"`）——
  `querySelectorAll` 匹配 0 个元素，模式切换、导出、代码页签全部无响应，
  但页面显示完全正常。已改为 `#view-prism`。
- 二维模式的指标标签未刷新（`面/边数` 应为 `三角形面数`）、
  板宽只显示单值（`20.000 mm` 应为 `20.000 × 20.000 mm`）—— 同属上述作用域缺陷的连带表现。
- **相机自动取景只做一次**：原逻辑用一个一次性开关 `cameraInit`，仅在首次进入本视图时
  拟合一次；此后切换一维/二维、或把板长从 50 改成 120，画面仍停在上一次的距离上 ——
  模型要么溢出视口、要么缩成一条细线，且**没有任何报错或提示**。改为按「取景签名」
  （模式 + 包围盒尺寸 + 中心）判定是否重新拟合：签名变化才重取景，用户手动旋转/缩放
  不会被强行拉回；「重置视角」「恢复默认」按钮改为清除签名以强制重取景。
- 取景算法由「包围球 + 视场角反算」改为**投影包围盒迭代拟合**：棱镜板是 20×50×0.45 的
  极扁长条，包围球法会因球半径远大于模型实际投影而把画面拉得过远。改为把包围盒 8 个
  角点投到相机坐标系、逐轴量出所需半角再反算距离，并统一留 12% 边距。

### 新增（测试）
- 新增测试套件 `tests/prism-check.js`（38 项），守三类"并入即埋雷"的失效：
  ① 不得回退到 CDN；② 颜色必须走 `--c-*` 变量；③ **选择器作用域必须命中
  真实 DOM 类名**（把本轮踩到的 `.prism-view` 缺陷钉死）。
- 冒烟测试新增 24 项棱镜断言（45 → 69）：画布尺寸贴合宿主、二维模式按钮真点击、
  二维体积 113.3 mm³（基底 80 + 金字塔 33.33）、STEP 生成不抛错，以及
  **自动取景回归**（切模式/改尺寸后取景签名必须改变；取景后居中偏移 ≤ 40px、
  四边余量为正、垂直占比 ≥ 50%）。
- `js/prism.js` 增加 `Prism.probe()` 调试接口：把模型包围盒 8 角点投到屏幕，
  直接输出屏占比与居中偏移，使取景正确性可被**断言**而非靠截图目测。

## [v3.1.0] - 2026-09-17

### 变更
- **被照面半径 R 改为自动跟随「半光强光斑半径」= h·tan θ½**，不必再点
  「按半光强光斑填充」。改灯具距离 h 或光束角，R 立即对应变化。
  该链接已移除；输入框标签补注「= h·tan θ½ 自动」，并在悬停提示里说明行为。
- **手动输入仍优先**：R 一旦被手动修改，就不再被自动覆盖 —— 否则用户刚输的值
  会被下一次改 h 悄悄改掉。判定方式是「当前值是否仍等于上次自动算出的值」，
  点「恢复默认」回到跟随态。

### 新增（测试能力）
- **冒烟测试现在能验证交互行为**：生成注入探针的临时副本后渲染，在页面里真的
  跑一遍操作再取结果（此前只能 dump 初始 DOM，交互逻辑完全无从回归）。
  据此新增 7 项断言（38 → 45）：默认 R = 3·tan19° = 1.033、h→6 后自动变为
  2.066、h→3 后跟随回来、手动改过 R 后不被覆盖、按钮已移除、
  以及复位后 R 与 E_max 复原。

### 修复
- 探针改过页面状态却不复位，会污染同一份 DOM 上的静态断言
  （本轮实际发生，一次跑出 9 项误报）。探针末尾已加复位，并断言复位结果。

## [v3.0.1] - 2026-09-17

### 修复
- **被照面照度分布图（俯视伪彩）上的「½Imax 光斑 R=…」标注看不清。**
  该图横跨深蓝→青→黄→红整个色域，原实现把 `rgba(255,255,255,.8)` 的裸白文字
  直接压在其上。参考圆与标注落在圆盘边缘，该处底色是 `rgb(54,177,139)` 的青绿，
  实测**文字对比度仅 2.22:1**（WCAG AA 正文要求 4.5:1）、参考圆白线 2.56:1。
  改为**深色圆角底衬 + 纯白文字**（对比度 **15.25:1**）；参考圆改为
  「先描深色再描白」的双描边（7.7:1）。

### 验证
- 画布像素实测：底衬像素 460 个（相对亮度低至 **0.0018**）、白字笔画 428 个
  （亮度 **1.0**），实测对比度 **20.26:1**（WCAG AAA 要求 7:1）
- 主题一致性新增 6 项断言：按 WCAG 相对亮度公式，取「底衬叠在最亮伪彩色
  `rgb(240,206,48)` 上」这一**最坏底色**计算文字与参考圆的对比度；并断言
  底衬不透明度 ≥ 0.6、双描边与底衬的实现确在代码中。该项由 34 项增至 40 项

### 说明
- 这类缺陷读代码不易发现：**伪彩图是全色域，任何单一文字色都必然在某些区域
  对比度不足**。凡是叠加在彩色图上的元素，都要自带底衬或描边，并按最坏底色
  核算对比度 —— 现已成为受断言守护的规则。

## [v3.0.0] - 2026-09-17

### ⚠️ 破坏性变更
- **光束角输入口径由「半光强半角 θ½」改回「半光强全角 2θ½」**（数据手册上写的
  "光束角"）。输入框标签、默认值、合法区间随之变化：

  | | v2.x | v3.0.0 |
  |---|---|---|
  | 输入字段 | 半光强半角 θ½ | **半光强全角 2θ½** |
  | 默认值 | 19 | **38** |
  | 合法区间 | 0.5° ~ 89.5° | **1° ~ 179°** |
  | 内部换算 | 直接使用 | **除以 2 得到 θ½** |

  **同一数值在新版语义不同。** 若把数据手册标注的**半角**值（如 19°）直接填入，
  新版会当作全角 19°（即半角 9.5°）。实测后果（Φ = 9000 lm、h = 3 m）：

  | 量 | 正确（填 38） | 误填（填 19） |
  |---|---|---|
  | 配光指数 n | 12.37 | **50.19**（放大 4.06 倍） |
  | 中心光强 I₀ | 19 155 cd | **73 330 cd** |
  | E_max | 2 128 lx | **8 148 lx（高估 282.8%）** |
  | 半光强光斑 R½ | 1.033 m | **0.502 m（低估）** |

  且页面不会有任何异常提示。按全角习惯填写（如 38°）则无需改动，
  结果与 v1.x 完全一致。

### 变更
- 口径防错方向反转：实时读数由「等价全角 2θ½」改为「等价半角 θ½」；
  常驻提示由「若标全角请除以 2」改为「若标**半角**（如 19°、12°）请先**乘以 2**」
- 理论页「记号约定」补上 2θ½ 的定义与「内部除以 2」的换算说明
- 冒烟测试新增 1 项断言（输入框默认值与区间须与全角口径一致），共 38 项

### 说明
- 本次变更使输入口径**回到 v1.x 的惯例**（v1.x 同样以全角为默认口径，只是当时
  另带一个口径选择开关）。因此从 v1.x 升级是兼容的，仅与 v2.x 不兼容。

## [v2.1.0] - 2026-09-17

### 变更
- **全站视觉改版为深色主题**，设计语言参照 [LLC 设计工具](https://wisdomhuang2020.github.io/LLC-Design-Tool/)：
  近黑底（`#0a0a0a` / `#171717` / `#262626`）+ teal 主色（`#0f766e` / `#14b8a6`）
  + amber 强调色（`#f59e0b`）；圆角由 12/8px 收敛为 8/6px；字体改用 Inter / JetBrains Mono
- 语义色集中为 `--c-*` CSS 变量；`index.html` 的 18 处图例色块改为引用变量，
  三个 canvas 模块的常量与之一一对应
- 三维视图的线框层次随主题反转：浅色主题下"后半更亮、前半更暗"，
  深色主题下改为"近处比远处亮"，否则透视关系颠倒

### 新增
- `tests/theme-check.js`（34 项断言）：断言 CSS 变量与 js 常量的对应关系、
  图例不硬编码颜色、深色主题无浅色残留
- 打印样式：深色页面直接打印会几乎全黑，已反转为白底黑字

### 修复
- 图例色块与 canvas 绘图用的是同一批颜色，此前是两处独立维护的字面量，必然漂移。
  改为变量引用 + 断言守护。**实测已抓到**：`--c-axis` 在 CSS 是 `#737373`，
  `solidangle.js` 写 `#7a7a7a`，`planar.js` 写 `#6b6b6b`，三个值互不相同。
- 伪彩照度图的色标低值端由 `rgb(6,28,82)` 提亮为 `rgb(18,42,104)`，
  并给图加亮灰外框 —— 原值在深色画布底上明度太接近，图形边界看不出来

### 外部依赖
- 新增 Google Fonts（Inter / JetBrains Mono），LLC 同款字体栈。这是本项目
  **唯一的外部依赖**；加载失败时回退到系统字体，不影响功能，如需离线自持可整段移除。

## [v2.0.2] - 2026-09-17

### 新增
- 版本号单一来源 `js/version.js`，页头徽标与页脚均实时显示当前版本
- 本文件 `CHANGELOG.md`，补齐 v1.0.0 起的全部版本记录
- `tests/version-check.js`：断言版本号在 `js/version.js`、本文件、页面三处一致，
  并断言 `index.html` 内不存在硬编码版本号

### 修复
- **页头徽标与页脚各写了一份硬编码 `v1.0.0`**，而实际已迭代至 v2.0.1，
  同一份 HTML 内两处同时漂移，页面显示的版本与实际版本不符。改为统一由
  `js/version.js` 注入，并用 class 钩子覆盖全部显示点 —— 若用 id 只能注入一处，
  将来新增显示点必然再漏。

## [v2.0.1] - 2026-09-17

### 变更
- 同步更新受影响的预览图（`preview/01-照度计算.png`、`preview/04-公式与推导.png`），
  使其与线上一致。仅预览资源，无代码变化。

## [v2.0.0] - 2026-09-17

### ⚠️ 破坏性变更
- **移除光束角的「半光强全角 2θ½ / 半角 γ½」口径开关**，输入字段统一为半光强
  半角 θ½。默认值由 38 改为 19，合法区间由 1~179 改为 0.5~89.5。
  同一数值在新版语义不同：照旧口径填写的参数会**静默出错**。例如把数据手册上
  的全角 38° 当半角填入，配光指数 n 由 12.37 降至 2.91、E_max 由 2 128 lx
  降至 622 lx（**低估 71%**），且页面不会有任何异常提示。

### 新增
- 口径防错（去掉开关会引入误填风险，而 38° 落在 θ½ 的合法区间内，校验拦不住）：
  输入框下方实时显示「等价全角 2θ½」，并常驻提示「数据手册若标的是全角，
  请先除以 2 再填入」。仅提示，不恢复开关。
- 记号统一：γ = 光线与光轴夹角，θ½ = 半光强半角
- 浏览器冒烟测试由 28 项增至 33 项，新增断言钉住防错行为

## [v1.1.0] - 2026-09-17

### 新增
- `tests/smoke-render.js`：无头 Chrome 渲染后导出 DOM，断言关键计算值与图表/表格
  是否真的渲染出来（28 项）。此类缺陷语法检查与单元测试均无法捕获，本项目已实际
  踩过两次：`addColorStop` 浮点越界致数据表整段消失、Canvas 初始化在隐藏视图内
  尺寸为 0 停在 300×150
- `tests/run-all.js`：统一测试入口，CI 与本地共用，避免两边跑的套件不一致
- `.github/workflows/test.yml`：可复用工作流（`workflow_call`），测试定义单一来源
- `.github/workflows/ci.yml`：push 非 main + PR + 手动，补上原本完全缺失的 PR 检查

### 变更
- `pages.yml` 改为 `test → build → deploy`，测试不通过则部署中止，坏版本无法上线
- 固定 Node 22；两处工作流均加 `concurrency`

## [v1.0.1] - 2026-09-17

### 修复
- `tools/push-via-api.js` 头注释原称「github.com 不可达」，实测只有 HTTPS(443)
  被墙、SSH(22) 一直可用。该结论会误导使用者绕过更简单的 SSH 方案，已更正为
  「首选 SSH、API 仅作降级通道」。纯注释修正，无功能变化。

## [v1.0.0] - 2026-09-17

### 新增
- **照度计算**：`cosⁿ` 余弦幂配光，由总光通量 Φ 与半光强角反求峰值光强 I₀，
  输出 E_max / E_min / E_avg、均匀度 U 与光通量利用率；附径向剖面曲线、
  俯视伪彩分布图与径向剖面数据表
- **立体角可视化**：可旋转三维球面（球冠 / 微圆环 / 微元方块 / 锥面线 / 光轴
  五个元素独立开关）；平面轴截面图（含纬圈投影弦）；环带一重积分与球面微元
  二重积分两条独立推导路径，互为交叉验证
- **公式与推导**页
- `tests/verify.js`：计算内核 41 项数值断言
- GitHub Pages 部署

### 已知限制
- 点光源近似，要求距离 ≥ 5× 发光面尺寸
- 单灯、无相互反射；不支持灯具倾斜（下照角 ≠ 90°）与竖直墙面
- 不解析 IES/LDT 配光文件；不含维护系数
- 不做法规判定，正式照明设计需 DIALux / AGi32 + 实测配光复核
