# 全站审核报告 · 2026-09-19

审核对象：`optical-calculator` 静态站点（index.html 109 KB / styles.css 55 KB / js 8 个模块 / 7 个页签）
版本基线：v3.10.1 → 审核后发布 **v3.10.2**

---

## 一、审核范围与方法

审核按五个维度展开，全部用可执行手段验证，不做"看起来没问题"的主观判断：

| 维度 | 手段 | 产物 |
|---|---|---|
| 仓库卫生与静态结构 | 正则扫描 id 唯一性、label 关联、标签闭合、资源引用、外链 | 手工扫描脚本 |
| 运行时错误与交互 | 无头 Chrome 遍历 7 页签，逐页捕获 JS 异常 / console.error / unhandledrejection，逐个触发 235 个控件，逐个量画布像素 | `tests/site-audit.js` |
| 计算内核与边界 | 对 optics / prism-perf 做 0 / 负数 / NaN / Infinity / 极小值的组合模糊测试 | `tests/_fuzz.js`（一次性） |
| 响应式与主题 | 1500 / 1280 / 1024 / 900 / 768 / 500 / 390 px 七个宽度下逐元素量横向溢出 | `tests/site-audit.js`（`WIDTH` 环境变量） |
| 可访问性 | 扫描 `<label>` 与控件的关联关系 | 手工扫描脚本 |

---

## 二、结论概览

| 等级 | 数量 | 状态 |
|---|---|---|
| P1 · 功能失效 / 性能缺陷 | 3 | 全部已修 |
| P2 · 显示错误 / 无障碍 | 2 | 全部已修 |
| P3 · 局部布局 | 2 | 全部已修（其一在上个版本 v3.10.1） |
| 通过项 | 5 类 | 无需改动 |
| 已知限制（未改） | 3 | 已在代码注释 / 页脚如实标注 |

---

## 三、缺陷清单

### P1-1 棱镜板设计页：WebGL 初始化失败会导致整页控件静默失效

**现象**：浏览器禁用硬件加速、老设备、或 WebGL 被策略拦掉时，三维预览创建失败，
于是**整页所有参数控件都不响应**，且页面不给出任何提示——用户只会以为"这页坏了"。

**根因**：`js/prism.js` 的 `init()` 里 `initThree()` 是裸调用，后面紧跟着
`bindControls() / refresh() / resize() / drawOnce()`。构造函数抛异常会直接跳过这四句：

```js
initThree();          // ← 抛错
bindControls();       // ← 永远执行不到
refresh();
```

**修复**：
- `initThree()` 包 try/catch，失败时置 `renderer = null` 并继续走完 `bindControls() / refresh()`，
  仅跳过 `resize() / drawOnce()`——二维图表、STEP 与脚本导出全部照常可用；
- 页面给出降级提示条："三维预览不可用（本机 WebGL 上下文创建失败：…）。参数计算与 STEP / 脚本导出仍可正常使用。"

**过程中发现的连带缺陷**：`refresh()` 末尾有一句 `showWarn('')`，会把刚设好的降级提示**清掉**。
已改成把"3D 不可用"作为独立状态并入 `showWarn()`，提示不再被后续刷新吞掉。

**验证**：无头 Chrome 自带 SwiftShader，正常路径永远走不到降级分支。因此审核套件用
`DEGRADE=1` 把 `THREE.WebGLRenderer` 换成必抛异常的桩，确定性复现：
控件改动仍能让脚本区更新 ✓、降级提示可见 ✓。

---

### P1-2 动画循环常驻：切走页签后仍在后台每帧重绘

**现象**：立体角页的自转 rAF 与棱镜页的 three.js rAF 都是无条件常驻循环。切到别的页签后，
画面用户根本看不见，CPU / GPU 与电量照耗（笔记本上表现为风扇常转）。

**根因**：
- `js/solidangle.js` 的 `running` 标志**从未被置为 false**（全仓搜索无赋值点）；
- `js/prism.js` 的 `animate()` 首行就是 `requestAnimationFrame(animate)`，没有停止条件。

**修复**：在 `js/app.js` 的 `activateTab()` 里加 `syncAnim(name)`——只让**当前**页签的渲染循环转：
立体角 `viz.running = (name === 'solid')`，棱镜页新增 `Prism.pauseAnim() / resumeAnim()`。
刷新带深链（如 `#prism`）或首屏停在默认页时同样对齐一次，避免漏网。

**验证**：审核套件在每个页签采集 `viz.running` 与 `Prism` 动画状态，与"是否当前页"比对，不符即判失败。

---

### P1-3 移动端布局崩塌：站点没有任何移动端断点

**现象**：CSS 原本只有 1080 / 1020 / 860 / 720 四个断点。窄到手机宽度时没有任何规则兜底，
整站横向滚动条常驻，元素大量越界。实测 496 px 视口下：照度计算页 100 个元素越界、
棱镜板设计页 87 个、材料参考页 58 个（合计 245），文档宽度被顶到 714 px。

**根因**（三条，叠在一起）：
1. **缺移动端断点**——720 px 以下没有任何规则；
2. `.stats` 等网格用 `1fr 1fr` 而非 `minmax(0, 1fr)`，grid item 的 `min-content` 无法压缩，
   硬把容器撑破；
3. **表格没有滚动容器**——材料参考页的表格摊到 720 px 无法收缩，把整个页面顶宽。

**修复**：新增 `@media (max-width: 620px)` 块，落在"棱镜知识页"注释**之前**
（`tests/knowledge-check.js` 有一条防污染断言：该注释之后不得出现无 `kn-` 前缀的重名类，
`.wrap` / `.stats` 若留在文件末尾会触发）。规则包括：
- 多列网格一律退成 `minmax(0, 1fr)` 单列；
- `.card / .panel { overflow-x: auto }` + `table { min-width: 460px }`——宽表在卡片内横向滚动，
  而不是把页面顶宽；
- `canvas { max-width: 100%; height: auto }`；
- `.row` 是 flex 不是 grid（第一版规则写错了选择器类型，已改为 `flex-wrap: wrap`）；
- `.tabs { overflow-x: auto }`——7 个页签在手机上必然放不下；
- 最优设计页的伪彩场与色标改为上下堆叠。

**验证**：文档级横向溢出 714 px → 496 px（完全消除）；剩余溢出元素经
`_scrollcheck` 逐个确认**全部落在可滚动容器内**，不是被裁切。

---

### P2-1 棱镜板设计页：非法输入把 NaN 印到页面上

**现象**：把齿距 / 齿高输入框清空或填成非数字，尺寸摘要（宽 b、收敛半角 β、体积、
半宽）直接显示 `NaN`；填 0 时齿距做除数，又是一处 `NaN`。
用户看到 NaN 只会认为"这站算错了"。

**根因**：`readParams()` 直接 `parseFloat($('p_pitch').value)`，不做有限性校验，
也不设正数下界。

**修复**：新增 `readNum(id, dflt, label, min)`：非有限值回退默认值并在输入提示区说明
"× × 不是有效数字，已按默认值 … 计算"；齿距 / 齿高 / 间距 / 结构高度等做除数的量加正数下界。
二维形状参数表（`p2_*`）同样处理。

**验证**：新增套件 `tests/nan-input-check.js`——7 个页签 × 5 种非法输入
（空 / `abc` / `0` / `-1` / `1e9`），遍历可见文本节点，出现 NaN 或 Infinity 即判失败。修复后全过。

---

### P2-2 42 个表单控件缺少可访问标签

**现象**：`<label>文字</label>` 后面跟一个 `<input>`，但 label 没有 `for`、控件也没有
`aria-label`——屏幕阅读器读不出这些输入框的含义。

**修复**：为 41 处游离 label 补 `for` 关联（全部指向存在的 id，无悬空引用）。
剩下 1 处是 radio 组（"被照面形状"），语义上不该指向单个 radio，改为给容器加
`role="radiogroup"` + `aria-label`。

**复核**：`label[for]` 总数 41，悬空 0；id 总数 235，重复 0。

---

### P3-1 768 px 下最优设计页表格溢出

`@media (max-width: 1020px)` 断点里补表格滚动容器与保底宽度，与移动端方案一致。

### P3-2 「棱镜原理」形状对照图右缘被截断

`viewBox` 宽 900 但六棱锥路径画到 932，超出视口被裁掉。已在 **v3.10.1** 修复（六形状重排 + 标签同步）。

---

## 四、通过项（无需改动）

- **静态结构**：235 个 id 无重复；`div / section / table / svg / g / canvas` 开闭标签计数平衡；
  13 个 `<script src>` 全部命中实际文件；无外链混杂（纯静态，零第三方请求）。
- **运行时**：修复后 7 个页签 × 235 个控件逐个触发，JS 异常 0、console 错误 0、
  未捕获 Promise 0、空输出区 0。
- **画布**：逐张量像素确认真的绘制了内容（不是只设了尺寸）。
- **主题**：明暗两套语义色对齐（由既有 `theme-check` 套件覆盖）。
- **仓库卫生**：根目录 3 个临时探针 HTML（`__probe-*.html`，共 350 KB）已清理；
  `.gitignore` 已覆盖此类产物，不会误入库。

---

## 五、已知限制（本次不改，已如实标注）

1. **计算内核不做防御性钳制**。模糊测试显示 `optics.js` 在 0 / 负数 / NaN 组合输入下
   会返回 NaN（如 `illuminance(0, 0)` 是 0/0）。这是内核层的设计取舍——数值函数不该
   悄悄替调用方改输入；钳制责任在 UI 层，而 UI 层已实测挡住（见 P2-1 验证）。
2. **腔壁按镜面处理**（最优设计页的镜像源法）。真实灯具侧壁是漫反射白色 PET。
   试算过"漫反射展宽"修正：在可及 OD 区间只带来 1~4 pp 差异，却引入一个任意参数，
   故回退。已在 `js/optimal-design.js` 注释中标注为已知简化。
3. **棱镜性能代理只校准过一个点**（金字塔 α=45°、n=1.49 处的蒙特卡洛），
   其余形状 / 角度是形状因子单调外推，不是实测。已在页脚如实写明。

---

## 六、新增的回归能力

审核不能是一次性的——发现的问题都固化成了可重复执行的断言：

| 套件 | 覆盖 |
|---|---|
| `tests/site-audit.js`（新） | 逐页遍历、控件体检、画布像素、横向溢出、动画预算、无 WebGL 降级 |
| `tests/site-audit.js` `DEGRADE=1`（新） | 模拟 WebGL 上下文创建失败，确定性验证降级路径 |
| `tests/nan-input-check.js`（新） | 非法输入不得显示 NaN / Infinity |

三者均已注册进 `tests/run-all.js`，发现问题返回非零退出码。套件总数 13 → 16。

复用方式：

```bash
node tests/site-audit.js              # 默认 1500 px
WIDTH=390 node tests/site-audit.js    # 换断点复检
DEGRADE=1 node tests/site-audit.js    # 模拟无 WebGL
node tests/nan-input-check.js
```

> 注：`site-audit.js` 单轮要 1~6 分钟——无头 Chrome 走 SwiftShader 软件光栅化，
> 棱镜页的 three.js 场景很吃 CPU。`execFileSync` 的超时已从 300 s 提到 600 s，
> 否则会把"跑得慢"误判成"失败"。
